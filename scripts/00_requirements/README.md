# 00_requirements

## Purpose
Verify the host system meets LFS build requirements.

## Usage
```bash
bash scripts/00_requirements/check_requirements.sh
```

## Notes
Run as root or a user with access to the required tools.
